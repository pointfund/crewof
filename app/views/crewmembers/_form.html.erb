<%= form_with(model: crewmember, local: true) do |form| %>
  <% if crewmember.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(crewmember.errors.count, "error") %> prohibited this crewmember from being saved:</h2>

      <ul>
      <% crewmember.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :address %>
    <%= form.text_field :address %>
  </div>

  <div class="field">
    <%= form.label :phone %>
    <%= form.text_field :phone %>
  </div>

  <div class="field">
    <%= form.label :email %>
    <%= form.text_field :email %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
